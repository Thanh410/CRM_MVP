# Daily Log – 2026-03-21

---

## 1. Fix khởi động dự án

### Vấn đề
- API server không start được do Prisma schema có **15 lỗi validation** (polymorphic FK relations sai)
- `node_modules` chưa có → cần `pnpm install`

### Đã fix
- **Prisma schema** (`apps/api/prisma/schema.prisma`): Xóa toàn bộ FK relations sai trong các model polymorphic (`Note`, `Activity`, `EntityTag`, `Attachment`, `CustomFieldValue`). Các model này dùng `entityType + entityId` ở application level, không có FK constraint xuống DB.
- **Xóa inverse relation arrays** khỏi `Lead`, `Contact`, `Company`, `Deal` (notes, activities, attachments, tags, customFieldValues).
- **Drop FK constraints trực tiếp trên DB** vì migration cũ đã tạo sẵn: `note_lead_fk`, `note_contact_fk`, `note_company_fk`, `note_deal_fk`, `act_lead_fk`, `act_contact_fk`, ... (20 constraints tổng)

---

## 2. Fix TypeScript errors (54 → 0)

| File | Lỗi | Fix |
|------|-----|-----|
| `crm/leads/leads.service.ts` | `include: { tags, notes }` không tồn tại | Xóa |
| `crm/contacts/contacts.service.ts` | `include: { tags }`, `Contact.avatar` sai | Xóa |
| `crm/companies/companies.service.ts` | `include: { tags, notes }` không tồn tại | Xóa |
| `crm/deals/deals.service.ts` | `include: { tags, notes }` không tồn tại | Xóa |
| `conversations/conversations.service.ts` | `assignedToId` → `assignedTo`, `Contact.avatar` sai, `lead` relation không tồn tại | Fix |
| `projects/projects.service.ts` | `department` → `dept`, import `@nestjs/mapped-types` → `@nestjs/swagger` | Fix |
| Nhiều file | Enum `AuditAction`, `EntityType`, etc. chưa generate | `prisma generate` |

---

## 3. Fix Frontend CSS / Config

| File | Vấn đề | Fix |
|------|--------|-----|
| `apps/web/postcss.config.js` | **Thiếu file** → Tailwind không compile | Tạo mới |
| `apps/web/tailwind.config.js` | `.ts` không support trong Next.js 14.2.4 | Rename sang `.js` |
| `apps/web/next.config.mjs` | `.ts` không support | Rename sang `.mjs` |
| `apps/web/tsconfig.json` | Thiếu `baseUrl` + `paths` → `@/` alias lỗi | Thêm vào |
| `apps/web/package.json` | `@radix-ui/react-badge` không tồn tại trên npm | Xóa, thêm `tailwindcss-animate` |

---

## 4. Auth – Remember Me + Forgot/Reset Password

### Backend (apps/api/src/modules/auth/)
- Thêm `POST /auth/forgot-password`: tạo JWT reset token (1h), log URL ra console (dev mode)
- Thêm `POST /auth/reset-password`: verify token, bcrypt hash password mới, revoke toàn bộ refresh tokens cũ
- Thêm DTOs: `ForgotPasswordDto`, `ResetPasswordDto`

### Frontend (apps/web/src/)
- **`store/auth.store.ts`**: Bỏ zustand `persist`, tự manage `localStorage`/`sessionStorage`. `rememberMe=true` → localStorage (persist qua restart); `rememberMe=false` → sessionStorage (mất khi đóng tab)
- **`lib/api.ts`**: Cập nhật interceptor đọc token từ `crm-auth` key (hỗ trợ cả 2 storages), auto-refresh 401
- **`app/(auth)/login/page.tsx`**: Thêm checkbox "Ghi nhớ đăng nhập", link "Quên mật khẩu?"
- **`app/(auth)/forgot-password/page.tsx`**: Form nhập email → success state "kiểm tra email"
- **`app/(auth)/reset-password/page.tsx`**: Form password mới + confirm, đọc `?token=` từ URL

---

## 5. API Checklist (API_CHECKLIST.md)

Kiểm tra **96 endpoints** toàn bộ backend. File: `API_CHECKLIST.md`

Kết quả: ✅ Tất cả pass sau khi fix các lỗi trên.

Bugs phát hiện và fix trong quá trình test:
- `POST /notes` trả 500 → DB còn FK constraints cũ → DROP CONSTRAINT
- `POST /marketing/campaigns` trả 400 → `CreateCampaignDto` thiếu field `description` → Thêm vào DTO

---

## 6. Danh sách API frontend đang dùng

Tổng: **29 API calls** trên 10 file. Xem bảng chi tiết trong `API_CHECKLIST.md`.

Phát hiện **66/92 endpoints chưa có frontend** (72% chưa kết nối).

---

## 7. Implement 4 tính năng Frontend còn thiếu

### 7.1 Notifications Bell
- **`hooks/use-notifications.ts`**: `useNotifications` (poll 30s), `useMarkRead`, `useMarkAllRead`
- **`components/layout/header.tsx`**: Dropdown notifications với badge số chưa đọc, "Đánh dấu tất cả đã đọc", empty state

### 7.2 Dashboard – Data thật
- **`app/(dashboard)/dashboard/page.tsx`**: Rewrite từ placeholder tĩnh → gọi 3 APIs:
  - `GET /reporting/dashboard` → 4 stat cards
  - `GET /reporting/sales-funnel` → bar chart phễu bán hàng (màu theo stage)
  - `GET /reporting/leads-by-source` → bar chart lead theo nguồn
  - Thêm badge breakdown trạng thái lead

### 7.3 Contacts + Companies CRUD đầy đủ
- **`app/(dashboard)/contacts/page.tsx`**:
  - Create/Edit modal: form đầy đủ (fullName, email, phone, mobile, jobTitle, company, assignee, address, description)
  - Dropdown công ty (`GET /companies?limit=50`) + dropdown nhân viên (`GET /users?limit=50`)
  - Slide-over detail: contact info + deals list
  - Delete với `window.confirm`
  - Pagination
- **`app/(dashboard)/companies/page.tsx`**:
  - Create/Edit modal: 9 fields (name, industry, size, website, taxCode, phone, email, address, description)
  - Slide-over detail: company info + contacts list + deals list
  - Delete với `window.confirm`

### 7.4 Notes + Activities Timeline
- **`components/entity-timeline.tsx`** (component dùng chung):
  - Tabs: "Tất cả / Ghi chú / Hoạt động"
  - Tạo Note mới (textarea + Ctrl+Enter)
  - Tạo Activity: type selector (CALL/EMAIL/MEETING/NOTE/TASK/OTHER icon pills) + title
  - Delete note/activity per item
  - Merge + sort theo `createdAt` DESC
- **`app/(dashboard)/leads/page.tsx`**: Click row → panel bên phải với contact info + EntityTimeline. Columns tự co lại khi panel mở. Thêm nút Xóa lead.
- **`app/(dashboard)/deals/page.tsx`**: Click deal card → panel bên phải với deal info + EntityTimeline. Deal card highlight khi selected.

---

## Tổng số file đã thay đổi hôm nay

| Loại | Số file |
|------|---------|
| Backend (NestJS) | 18 file |
| Frontend (Next.js) | 12 file |
| Config/Infra | 5 file |
| Docs | 2 file (API_CHECKLIST.md, file này) |
| **Tổng** | **37 file** |

---

## Trạng thái hiện tại

| Module | Backend | Frontend |
|--------|---------|----------|
| Auth (login/logout/refresh/forgot/reset) | ✅ | ✅ |
| Leads (CRUD + export + convert) | ✅ | ✅ + Timeline |
| Contacts (CRUD) | ✅ | ✅ CRUD đầy đủ |
| Companies (CRUD) | ✅ | ✅ CRUD đầy đủ |
| Deals (Kanban + stage + won/lost) | ✅ | ✅ + Timeline |
| Tasks (Kanban + CRUD) | ✅ | ✅ |
| Projects | ✅ | ⚠️ chỉ list |
| Marketing (campaigns + templates) | ✅ | ⚠️ campaigns only |
| Conversations/Inbox | ✅ | ✅ |
| Notes + Activities | ✅ | ✅ (via EntityTimeline) |
| Tags | ✅ | ❌ chưa có UI |
| Notifications | ✅ | ✅ bell + dropdown |
| Reporting/Dashboard | ✅ | ✅ |
| Users/RBAC | ✅ | ❌ chưa có UI |
| Organizations/Settings | ✅ | ❌ chưa có UI |
| Audit Log | ✅ | ❌ chưa có UI |

---

## Việc còn lại (backlog)

1. **Tags UI** – gán/bỏ tag cho leads, contacts, deals
2. **Projects page** – CRUD đầy đủ, không chỉ dropdown
3. **Marketing Templates** – UI quản lý templates
4. **Users page** – danh sách user + invite + phân quyền
5. **Settings page** – org info, departments, teams
6. **Audit Log page** – xem lịch sử thao tác (admin)
7. **Deal detail** – edit form đầy đủ (giá trị, ngày chốt, lý do thua)
8. **Leads import CSV** – upload file
9. **Conversation assign** – gán agent trong inbox
10. **Email service thật** – thay `console.log` bằng SMTP/Resend cho forgot password
